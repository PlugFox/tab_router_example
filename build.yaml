targets:
  $default:
    sources:
      - $package$
      - pubspec.yaml
      - lib/**
      - test/**
    builders:
      pubspec_generator:
        options:
          output: lib/src/common/constants/pubspec.yaml.g.dart